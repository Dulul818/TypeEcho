# 更新记录

## 1.0.0 · 2026-09-18

首个正式发行版，产品名称为 **TypeEcho 键语**，以已验证的 0.4.18 功能为基础。

- 保留旧扩展 ID、命令 ID、设置键和数据目录，直接升级可继续使用已有登录、偏好、进度与报告。
- 源码收拢至 `src/`，语音模块放入 `src/speech/`，界面模板和参考面板放入 `src/views/`。
- 练习页 HTML 与宿主会话控制分离；浏览器预览读取复用已知下标，减少重复扫描。
- 安装包统一输出到 `releases/`，文件名从版本号生成；补齐仓库元数据、第三方许可及开发构建配置。
- 重写使用与模型接入说明，新增开发、自测和持续集成流程。
- 模型、运行环境、个人配置、日志和测试输出不进入源码仓库及安装包。

### 保留的功能与修复

- 五种阅读位置、固定字形反馈、文件接力、进度恢复、报告与官网回执。
- 逐词 / 按句 / 混合模式、智能补词、短句段去重、标点保护与完整逐词排队。
- 异步整局句段预生成、后续 32 词预生成、128 段 / 32MiB 普通缓存及失败重试。
- GPT-SoVITS v2ProPlus、参考音频原音试听、字幕、导入与切换。
- 底部输入光标可隐藏；GPT-SoVITS 单词额外静音由 300ms 减为 30ms，完整发音保持不变。

## 0.4.18 及以前

原名 Code Type Bridge，为本机迭代版本。0.4.14 接入 GPT-SoVITS，0.4.15 增加参考音频面板，0.4.16 扩大逐词预生成，0.4.17 增加光标显示开关，0.4.18 缩短单词尾部额外静音。

旧版完整说明及本机部署记录保留在开发机器的 `.local/history/`，不作为当前安装指南。
